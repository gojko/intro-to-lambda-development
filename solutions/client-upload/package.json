{
  "name": "upload-signer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "create": "claudia create --version dev --api-module api --region $npm_package_config_region --configure-bucket --no-optional-dependencies",
    "deploy": "claudia update --version dev --cache-api-config=uploaderConfig",
    "reconfigure": "claudia update --version dev --configure-bucket"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "config": {
    "region": "us-east-1"
  },
  "dependencies": {
    "claudia-api-builder": "^4.0.0"
  },
  "devDependencies": {
    "claudia": "^4.0.0"
  },
  "optionalDependencies": {
    "aws-sdk": "^2.234.1"
  }
}
